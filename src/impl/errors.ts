/**
 * Map of error codes to messages for schema manipulation
 */
export const schemaErrors: { [P: string]: string } = {
    CLASS_NOT_FOUND: "Class '{class}' not found",
    CLASS_ALREADY_EXISTS: "Class '{class}' already exists",
}